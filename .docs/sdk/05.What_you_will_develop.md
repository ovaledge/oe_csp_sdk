# 5. What you will develop

A connector is a **Maven module** that:

1. Implements **AppsConnector** (connection validation, metadata, query, server type, connection attributes).
2. Provides **MetadataService** (supported object types, containers, objects, fields).
3. Provides **QueryService** (executes queries and fetches data for an entity).
4. Is registered in `META-INF/services/com.ovaledge.csp.v3.core.apps.service.AppsConnector` so the SDK and `csp-api` can discover it via ServiceLoader.

Optionally, it can include a **quick** package: a small Spring Boot app to run and test that connector alone (e.g., health, connection validate, supported-objects). Review the `monetdb` and `awsconsole` connectors as reference implementations.

## 5.1 Connector Contract

For implementation, treat the [Connector Interfaces Reference](../Connector_Interface_Reference.md) as the source of truth for:

- Required vs. optional/default methods
- Method inputs, outputs, errors, and invariants
- Lifecycle execution order
- Thread-safety and performance expectations

## 5.2 Connector Interfaces

### 5.2.1 AppsConnector (from csp-sdk-core)

Your connector class must implement:


| Method                                                     | Purpose                                                                                |
| ---------------------------------------------------------- | -------------------------------------------------------------------------------------- |
| `getServerType()`                                          | Unique string used in requests (e.g., `"monetdb"`, `"myapp"`). Use lowercase.          |
| `validateConnection(ConnectionConfig config)`              | Validate credentials/settings; return success/failure.                                 |
| `getMetadataService()`                                     | Return your `MetadataService` implementation.                                          |
| `getQueryService()`                                        | Return your `QueryService` implementation.                                             |
| `getAttributes()`                                          | Connection form definition (host, port, username, password, etc.) for the OvalEdge UI. |
| `exchangeAttributes(ConnInfo)` / `exchangeAttributes(Map)` | Map between UI attributes and ConnInfo / config.                                       |


Optional: override `getProfilingService()` to return a `ProfilingService` when profiling is declared. Default is `null`. See [Connector Interface Reference §4.4](../Connector_Interface_Reference.md#44-profilingservice-optional-contract).

Additional methods (`getExtendedAttributes`, `exchangeVaultAttributes`, `getVaultPath`, etc.) are used for secrets/vault and governance; you can delegate to `BaseAppConnector` for common behavior (see `monetdb`).

### 5.2.2 MetadataService


| Method                             | Purpose                                                                   |
| ---------------------------------- | ------------------------------------------------------------------------- |
| `getSupportedObjects()`            | List supported entity types (e.g., entities, reports).                    |
| `getContainers(ContainersRequest)` | List top-level containers (e.g., schemas, companies, accounts).           |
| `getObjects(ObjectRequest)`        | List objects (e.g., tables, entities, reports) in a container for a type. |
| `getFields(FieldsRequest)`         | List fields (columns) for a given object.                                 |


### 5.2.3 QueryService


| Method                    | Purpose                                                             |
| ------------------------- | ------------------------------------------------------------------- |
| `fetchData(QueryRequest)` | Execute the query (filters, fields, limit, offset) and return rows. |


## 5.3 Required vs. Optional (Implementation Checklist)

- **Required to implement:** `AppsConnector`, `MetadataService`, `QueryService` contracts used by runtime routing and query flow.
- **Optional/default:** `getProfilingService()` (default `null`; see [Connector Interface Reference §4.4](../Connector_Interface_Reference.md#44-profilingservice-optional-contract)), EDGI and SDK version methods when the connector does not support those workflows.
- **Strongly recommended:** Annotate the connector with `@SdkConnector(artifactId="...")` and include a release metadata file for runtime auditability.

## 5.4 Error Handling Conventions

Connectors must handle errors predictably so the OvalEdge runtime can report meaningful messages to end users. Follow these conventions:

- **Connection failures:** Throw or return a structured error response from `validateConnection()` with a human-readable message. Do not swallow exceptions silently.
- **Metadata errors:** If a container or object cannot be listed (e.g., insufficient permissions), return an empty result with a log warning rather than throwing. Only throw for fatal, unrecoverable errors.
- **Query errors:** Return structured error responses from `fetchData()` including the error message and, where possible, the failing query or entity reference.
- **Timeouts:** Honor the timeout values in `ConnectionConfig` or `QueryRequest`. Surface timeout exceptions with a clear message, including elapsed time.
- **Unchecked exceptions:** Wrap all third-party SDK or JDBC exceptions in OvalEdge connector exception types. Do not propagate driver-specific exceptions to the runtime.

## 5.5 Logging Guidelines

Use the SLF4J logger provided by the SDK base classes. Follow these conventions:


| Level   | Use for                                                                                   |
| ------- | ----------------------------------------------------------------------------------------- |
| `ERROR` | Unrecoverable failures (authentication failure, network unreachable, fatal config error). |
| `WARN`  | Recoverable issues (empty result set, deprecated config field, retried connection).       |
| `INFO`  | Connection lifecycle events (connected, disconnected, query completed).                   |
| `DEBUG` | Request/response details, query parameters, field mappings — enabled only in development. |


**Never log credentials, tokens, passwords, or personally identifiable information (PII) at any log level.** Mask sensitive values before logging connection attributes.

## 5.6 SDK scope and limitations

SDK connectors are built for **connection validation**, **metadata discovery** (catalog/crawl), **sample data preview**, and optional **profiling** (when you implement `ProfilingService`). Other OvalEdge capabilities exist on the platform; the table below states which are available for SDK connectors in the current release.

**Supported?** values:

- **Yes** — Available for SDK connectors in the current release.
- **No** — Not available for SDK connectors in the current release.
- **Partial** — Depends on what you implement in your connector and which crawler options you enable at registration (see §5.6.2).

### 5.6.1 Platform capabilities

| Capability | Supported? | Details |
| ---------- | ---------- | ------- |
| **Connector authoring** | Yes | Build a connector module with the SDK Connector Generator, register it, and test locally with the csp-api app. See §5.1–5.5 and the [Connector Interface Reference](../Connector_Interface_Reference.md). |
| **Connection validation** | Yes | Validate credentials and settings from the OvalEdge connection UI before crawl or sample data runs. |
| **Authentication & secrets** | Partial | You define which auth fields appear on the connection form (for example, username/password, API key, OAuth). Supported credential managers are declared in your connector configuration JSON. |
| **Catalog / crawl** | Yes | Discover and catalog metadata: containers/schemas, tables/entities, columns/fields. |
| **Full crawl** | Yes | Scheduled or on-demand jobs that load metadata into the Data Catalog. |
| **Incremental crawl** | No | Delta or incremental catalog refresh is not available yet—only full crawl. |
| **Sample Data** | Yes | Preview rows on the **Data Catalog** page for crawled tables/entities. Sample size follows crawler settings. |
| **Query Sheet** | Partial | Query Sheet is supported for JDBC Connectors built on SDK to run ad-hoc queries, save datasets, and execute queries. Use **Sample Data** in the catalog instead. |
| **Query policies** | No | Query-governance policies applied from the Query Sheet module. |
| **Query Sheet permissions** | No | Application-level or source-level permission modes for Query Sheet. |
| **Tables, views & columns** | Partial | Core database-style objects—enabled by default for most SDK database connectors. |
| **Reports & report columns** | Partial | Reporting/BI objects only if your connector implements them and enables the matching crawler options. |
| **Files & folders / buckets** | Partial | File-system-style containers and files only if your connector implements them and enables the matching crawler options. |
| **APIs & API attributes** | Partial | API endpoints and attributes only if your connector implements them and enables the matching crawler options. |
| **Procedures, functions & source code** | Partial | Routines and definitional metadata only if your source exposes them and your connector implements the crawl. |
| **Indexes & synonyms** | Partial | Index and synonym metadata only if supported by your source and implemented in the connector. |
| **Relationship metadata** | Partial | Foreign-key or link-style relationships in catalog metadata when the source exposes them—not the same as automatic **lineage** jobs. |
| **Source permissions crawl** | No | Crawling security permissions from the source into OvalEdge. |
| **Users & roles crawl** | No | Crawling database or application users and roles from the source. |
| **Profiling** | Partial | Implement optional `AppsConnector.getProfilingService()` (`ProfilingService`) for ENTITY/VIEW tabular profiling and/or FILE profiling. Test via **csp-api** `/profiling/*` (SPI). In this repo: **MonetDB** is the full DBMS reference (`monetdb.json` oasis-ready flags). Sample-only connectors use the csp-api generator **SAMPLE** stub (override `getRowCount` and `sampleProfile`; leave `profileColumn` unsupported). Connectors must throw `ProfilingUnsupportedException` for non-supported kinds (REPORT/DASHBOARD/FUNCTION/PROCEDURE/ETL/DATASET/FILEFOLDERS, etc.). **OvalEdge Profile jobs** still require the platform adapter ([CLB-22](https://ovaledge.atlassian.net/browse/CLB-22)); enabling flags before that adapter makes the OE Profile UI appear but jobs fail at `DtoProvider` until CLB-22. Full SPI, field mapping, identity, and CLB-22 adapter notes: [Connector Interface Reference §4.4](../Connector_Interface_Reference.md#44-profilingservice-optional-contract). Also §5.6.2. |
| **Data quality (DQ)** | No | Data quality rules and scoring that depend on profiling. |
| **Anomaly detection** | No | Anomaly detection jobs tied to profiling. |
| **Lineage (auto / build)** | No | Automatic lineage extraction and crawler jobs that build lineage from the source. |
| **Query log crawling** | No | Ingesting query or audit logs from the source for lineage or catalog. |
| **DAM / RDAM** | No | Remote data access management: access-control sync and remote policy fulfillment against the source. |
| **OvalEdge Bridge-only mode** | No | SDK connectors are integrated as standard application connectors, not as Bridge-only integrations. |
| **EDGI workspace data** | Partial | Optional; only if you explicitly add EDGI support in your connector. Not enabled by default. |

If a future release changes any **No** or **Partial** row, this table and the connector generator UI will be updated together.

### 5.6.2 What you must define for your source

The SDK does not automatically support every feature your source offers. Document these in your connector README:

| Topic | Your responsibility |
| ----- | ------------------- |
| **Object types** | Only object types you implement are cataloged (for example, tables only, or tables plus reports). |
| **Incremental extraction** | If the source has no delta API, state that OvalEdge runs **full crawl** on a schedule. |
| **Authentication** | List which sign-in methods work (and which do not) for your connection form. |
| **Data reads** | Sample Data uses structured reads for a **selected catalog object**, not free-form SQL or REST from end users. |
| **Crawler options** | Optional crawl features (reports, files, APIs, etc.) must be implemented **and** registered—users only see what you enable. |
| **Profiling** | Optional. Override `getProfilingService()` and implement `ProfilingService` for supported kinds only: ENTITY/VIEW (row count + column aggregate + sample + batch) and FILE (file parsing + per-column stats via `profileFile`). App connectors may support **sample profiling only** for crawled entities (OBJECT kind = ENTITY): implement `getRowCount` and `sampleProfile`, and throw unsupported for tabular aggregate (`profileColumn`) when not readable. OvalEdge jobs skip sample profiling if `getRowCount` throws or returns 0. Set JSON `profiling`/`sampleProfiling` true only when the SPI is implemented (oasis-ready registration). Do not introduce `ProfilingMode`; routing is handled internally via `ObjectKind` + request identity. Methods, result fields, batch modes, identity, and CLB-22 mapping: [Connector Interface Reference §4.4](../Connector_Interface_Reference.md#44-profilingservice-optional-contract). |

---

[⬅️ Previous: 4. Repository Structure](04.Repository_Structure.md)

[➡️ Next: 6. Generate the new Connector Source](06.Generate_the_new_Connector_Source.md)